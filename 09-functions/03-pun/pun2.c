/* Prints a bad pun */

#include <stdio.h>

void print_pun(void) // shows that you can declare void as the parameter. same would be as showing "print_pun()"
{
  printf("To C, or not to C");
}

int main()
{
  print_pun();
  return 0;
}